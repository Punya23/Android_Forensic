# Core service
flask>=3.0
flask-cors>=4.0
flask-socketio>=5.3
simple-websocket>=1.0
python-dotenv>=1.0.0    # loads engine/.env (SNAGR_AUTH_USER/PASS) so creds don't need shell export

# ADB orchestration
adbutils>=2.5.0

# Artifact parsing
Pillow>=10.0            # image + EXIF
piexif>=1.1.3          # robust GPS EXIF decode

# Report generation
Jinja2>=3.1
reportlab>=4.0.0
python-docx>=1.0.0
pandas>=2.0.0

# Notifications
twilio>=8.0.0
slack_sdk>=3.0.0
pydantic>=2.0.0
# Deleted-record recovery (optional but preferred; engine has a built-in fallback carver)
# sqlite-dissect>=1.0.0

# Dev / test
pytest>=8.0
