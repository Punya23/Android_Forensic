<?xml version="1.0" encoding="utf-8"?>
<!--
  eRakshak Collector — Tier-1 forensic helper.

  Declaring a permission does NOT grant it. On stock Android the engine grants each one
  explicitly (and logs it) via `pm grant`. On OEM builds where pm grant is blocked
  (e.g. OnePlus/OxygenOS — see commit 6485e5e) the app uses ActivityCompat.requestPermissions()
  to show a runtime dialog instead. Every collector reads only what it was actually granted
  and reports what it could not reach, so the acquisition is fully auditable.

  Permission → how the engine grants it (all logged, alters_device=true):
    READ_CONTACTS ............. pm grant / runtime dialog    (dangerous, not hard-restricted)
    READ_MEDIA_IMAGES/VIDEO/AUDIO  pm grant / runtime dialog (Android 13+ media groups)
    READ_EXTERNAL_STORAGE ..... pm grant                     (<= Android 12 media enumeration)
    ACCESS_MEDIA_LOCATION ..... pm grant                     (unredacted EXIF GPS from MediaStore)
    READ_CALENDAR ............. pm grant
    GET_ACCOUNTS .............. pm grant                      (best-effort; OEM-dependent on 8+)
    QUERY_ALL_PACKAGES ........ normal (install-time)         (full installed-app inventory)
    PACKAGE_USAGE_STATS ....... appops set GET_USAGE_STATS allow
    READ_CALL_LOG ............. hard-restricted → default-Dialer role swap (revert after use)
    READ_SMS .................. hard-restricted → default-SMS role swap  (revert after use)
    REQUEST_INSTALL_PACKAGES .. install-time (needed for Xiaomi HyperOS/MIUI USB install flow)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Storage (write JSON output + enumerate media on <= Android 12) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Media (Android 13+ split media permissions) + unredacted EXIF GPS -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Communications & PIM content providers -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Accounts, installed-app inventory, app-usage telemetry -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Xiaomi HyperOS/MIUI: 'Install via USB' enforcement needs this permission
         declared so the USB install flow works in Developer Options. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Wi-Fi (saved networks, current SSID, scan results) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth (bonded/paired devices — Android 12+ split permissions) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:minSdkVersion="31"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />


    <application
        android:allowBackup="false"
        android:label="eRakshak Collector"
        android:requestLegacyExternalStorage="true"
        android:theme="@android:style/Theme.Material.Light">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
