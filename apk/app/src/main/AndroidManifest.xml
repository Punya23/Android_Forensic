<?xml version="1.0" encoding="utf-8"?>
<!--
  eRakshak Collector — Tier-1 forensic helper.
  Declares the dangerous permissions it needs; declaring them does NOT grant them.
  Contacts/media are grantable via `pm grant` with no root. Call log & SMS are
  hard-restricted and require the temporary default-role swap documented in the README.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.erakshak.collector">

    <!-- Storage: needed to write JSON output to Downloads folder -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Content providers -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="false"
        android:label="eRakshak Collector"
        android:theme="@android:style/Theme.Material.Light">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
